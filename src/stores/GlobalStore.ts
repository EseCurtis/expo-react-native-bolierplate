import { Store } from "pullstate";

interface IGlobalStoreStore {
    
}

const GlobalStoreStore = new Store<IGlobalStoreStore>({
});

export const GlobalStoreStoreReducer = {
    
}

export default GlobalStoreStore;